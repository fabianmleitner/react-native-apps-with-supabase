interface:
  display_name: "Manager"
  short_description: "Coordinate multi-agent workflow execution"
  default_prompt: "Use $manager to coordinate this workflow and track each role's next action."
policy:
  allow_implicit_invocation: true
