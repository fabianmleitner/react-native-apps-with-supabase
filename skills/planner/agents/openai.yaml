interface:
  display_name: "Planner"
  short_description: "Break tasks into executable plans"
  default_prompt: "Use $planner to create a clear implementation plan for this task."
policy:
  allow_implicit_invocation: true
