import { HeroProgress } from '../../src';

const meta = {
  title: 'Design System/Organisms/HeroProgress',
  component: HeroProgress,
  parameters: {
    layout: 'centered'
  }
};

export default meta;

export const Default = {};
